{
    "name": "next-theme-json-setup",
    "version": "0.5.0",
    "description": "NExT theme.json Setup — development tooling (wp-env, e2e, linting).",
    "private": true,
    "scripts": {
        "prepare": "husky",
        "wp-env": "wp-env",
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui"
    },
    "lint-staged": {
        "*.php": "composer run phpcs --"
    },
    "devDependencies": {
        "@playwright/test": "^1.48.0",
        "@wordpress/e2e-test-utils-playwright": "^1.16.0",
        "@wordpress/env": "^10.10.0",
        "husky": "^9.1.0",
        "lint-staged": "^15.2.0"
    }
}
